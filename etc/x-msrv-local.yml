timeout: 2s
nsq:
    nsqd-tcp-address:
        - localhost:4150
    #lookupd-http-address:
    #    - localhost:4160
    topic: testtopic
    channel: testchannel
    max-in-flight: 10
    max-messages: 10
aerospike:
    address:
        host: localhost
        port: 3000
    namespace: test
    set: testset
